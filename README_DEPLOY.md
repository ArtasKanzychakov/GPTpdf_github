# 🚀 БИЗНЕС-НАВИГАТОР v7.1 (Финальная версия)

Интеллектуальный Telegram-бот для подбора бизнес-ниши на основе AI-анализа личности.

## ✨ Особенности

- 📊 **18 интерактивных вопросов**: текст, выбор, мультиселект, шкалы, слайдеры
- 🤖 **AI-анализ**: психологический профиль через OpenAI GPT-4
- 🎯 **Персональные рекомендации**: подбор ниши и детальный план
- 💳 **Готовые донаты**: ЮКасса, Stripe, Telegram Stars (опционально)
- 🔄 **Auto Keep-Alive**: система самопробуждения для Render.com

## 📁 Структура

```
business-navigator/
├── app.py                     # FastAPI сервер
├── requirements.txt           # Зависимости
├── runtime.txt               # Python 3.9.16
├── Dockerfile                # Docker образ
├── render.yaml               # Конфигурация Render
│
├── config/                   # Конфигурация
│   ├── settings.py          # Настройки бота
│   ├── questions.yaml       # Вопросы анкеты
│   ├── questions_v2.yaml    # Альтернативные вопросы
│   └── *.txt                # Промпты для AI
│
├── core/                    # Ядро бота
│   ├── bot.py              # Основной класс
│   └── question_engine_v2.py
│
├── handlers/                # Обработчики
│   ├── commands.py         # Команды (/start, /help)
│   ├── questionnaire.py    # Анкета
│   └── ui_components.py    # UI компоненты
│
├── services/               # Сервисы
│   ├── data_manager.py    # Управление данными
│   ├── openai_service.py  # OpenAI интеграция
│   └── payment_service.py # Платежи (готов к использованию)
│
├── models/                # Модели
│   ├── session.py        # Сессия пользователя
│   ├── enums.py          # Перечисления
│   └── question_types.py
│
└── utils/                # Утилиты
    ├── formatters.py    # Форматирование
    └── logger.py        # Логирование
```

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонировать репозиторий
git clone https://github.com/your-repo/business-navigator.git
cd business-navigator

# Создать виртуальное окружение
python3.9 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка

```bash
# Создать .env файл
cp .env.example .env

# Отредактировать .env:
# - TELEGRAM_BOT_TOKEN (от @BotFather)
# - OPENAI_API_KEY (от OpenAI)
```

### 3. Запуск локально

```bash
python app.py
```

Бот будет доступен на `http://localhost:10000`

## 🌐 Деплой на Render.com

### 1. Подготовка

```bash
# Убедитесь что все файлы на месте
git add .
git commit -m "Initial commit"
git push origin main
```

### 2. Создание сервиса

1. Зайти на [Render.com](https://render.com)
2. **New → Web Service**
3. Подключить GitHub репозиторий
4. Настройки:
   - **Name**: business-navigator
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`

### 3. Переменные окружения

Добавить в Render → Environment:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token
OPENAI_API_KEY=your_openai_key
OPENAI_MODEL=gpt-4-turbo-preview
PORT=10000
RENDER_EXTERNAL_URL=https://your-app.onrender.com
```

### 4. Деплой

Render автоматически задеплоит после push в main.

## 💳 Подключение донатов (опционально)

### ЮКасса (для РФ)

1. Регистрация: https://yookassa.ru/
2. Получить `SHOP_ID` и `SECRET_KEY`
3. Добавить в Environment:
   ```bash
   PAYMENT_ENABLED=true
   PAYMENT_PROVIDER=yookassa
   YOOKASSA_SHOP_ID=your_id
   YOOKASSA_SECRET_KEY=your_key
   ```
4. Раскомментировать код в `services/payment_service.py`

### Stripe (международные)

1. Регистрация: https://stripe.com/
2. Получить API ключи
3. Добавить в Environment:
   ```bash
   PAYMENT_ENABLED=true
   PAYMENT_PROVIDER=stripe
   STRIPE_SECRET_KEY=sk_live_...
   STRIPE_WEBHOOK_SECRET=whsec_...
   ```

## 📊 Мониторинг

### Health Check

```bash
curl https://your-app.onrender.com/health
```

Должен вернуть:
```json
{"status":"healthy","bot":"running","timestamp":"..."}
```

### Логи

Render → Dashboard → Your Service → Logs

## 🧪 Тестирование

```bash
# Health check
curl http://localhost:10000/health

# Ping
curl http://localhost:10000/ping

# API документация
open http://localhost:10000/docs
```

## 🎯 Типы вопросов

Бот поддерживает:

- **text** - свободный ввод текста
- **choice** - выбор одного варианта (кнопки)
- **multiple_choice** - выбор нескольких вариантов (галочки)
- **scale** - оценка по шкале (1-7)
- **slider** - слайдер с регулировкой значения

## 🤖 Команды бота

- `/start` - Начать работу
- `/help` - Помощь
- `/questionnaire` - Запустить анкету
- `/restart` - Перезапустить сессию
- `/status` - Статус бота

## 📝 Переменные окружения

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | ✅ Да |
| `OPENAI_API_KEY` | Ключ OpenAI API | ✅ Да |
| `OPENAI_MODEL` | Модель GPT | Нет (по умолчанию gpt-4-turbo-preview) |
| `PORT` | Порт сервера | Нет (по умолчанию 10000) |
| `PAYMENT_ENABLED` | Включить платежи | Нет (по умолчанию false) |

## 🔧 Разработка

### Структура кода

- **app.py** - точка входа, FastAPI приложение
- **core/bot.py** - основной класс бота
- **handlers/** - обработчики команд и сообщений
- **services/** - бизнес-логика (AI, платежи, данные)
- **models/** - модели данных
- **utils/** - вспомогательные функции

### Добавление новых вопросов

Редактировать `config/questions.yaml`:

```yaml
- id: "new_question"
  text: "Ваш вопрос?"
  type: "choice"  # text, choice, multiple_choice, scale, slider
  category: "category_name"
  options:
    - value: "opt1"
      label: "Вариант 1"
```

## 🐛 Решение проблем

### Бот не отвечает

1. Проверить `/health` endpoint
2. Проверить логи Render
3. Проверить `TELEGRAM_BOT_TOKEN`
4. Убедиться что бот не спит (UptimeRobot)

### OpenAI не работает

1. Проверить `OPENAI_API_KEY`
2. Проверить баланс OpenAI
3. Попробовать `gpt-3.5-turbo`

### Ошибка импорта

1. Проверить `requirements.txt`
2. Переустановить зависимости: `pip install -r requirements.txt --upgrade`

## 📄 Лицензия

MIT

## 👤 Автор

Business Navigator Team

## 🙏 Благодарности

- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot)
- [FastAPI](https://fastapi.tiangolo.com/)
- [OpenAI](https://openai.com/)

---

**Версия**: 7.1 (Final)
**Python**: 3.9.16
**Дата**: Февраль 2026